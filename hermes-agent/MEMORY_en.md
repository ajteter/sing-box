# MEMORY.md — Long-Term Memory

*Durable facts, preferences, and decisions. Loaded at the start of every DM session.*

## User Preferences

- Steam prefers concise, technical communication
- Always seek permission before modifying system configurations (especially: never change default Nginx ports)
- Modularity first: extract independent features into separate files, don't stuff everything into one main file

## Operating Rules

- Reading files requires no permission; modifications (deletions, writes, patches) and system config changes require explicit approval
- Bulk operations (multi-file modify/create/delete) require a proposed plan and confirmation before execution
- When requirements are ambiguous, ask for context instead of guessing

## Known Facts

*(Accumulate durable facts and decisions here as interactions progress)*

---

*Maintained by Hermes. Append new durable information to the appropriate section.*
